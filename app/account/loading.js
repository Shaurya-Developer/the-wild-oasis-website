import Spinner from "@/app/_components/Spinner";

export default function Loading() {
  return <Spinner />;
}
//  For lading data in whole app this component is rendered automatically bcz of loading.js convention in nextJS
